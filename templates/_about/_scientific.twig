{% extends "_layout.twig" %}

{% block content %}

<div class="bg-light py-10">
<div class="section section--default">
	<div class="container">
    <div class="mb-5">{% include '_snippet/_nav/_breadcrumb' %}</div>
		{% include '_snippet/_content/_pageTitle' %}
		{% include '_snippet/_content/_intro' %}
    </div>
</div>
</div>

<div class="section section--default">
	<div class="container">
        <h2 class="text-primary">Members</h2>
            <div class="flex flex-wrap -mx-4">
                {% set members = craft.entries.section('teamMembers') %}
                {% for member in members %}
                    {% include '_snippet/_item/_scientific' %}
                {% endfor %}
    </div>
</div>

{% include '_snippet/_content/_contentBuilder' %}

{% endblock %}