<div class="section section--default">
	<div class="container">
        <div class="bg-light px-10 py-10">
        <div class="text-primary text-center">
            <p>{{ block.text }}</p>
        </div>

            {% if block.writer %}
        <div class="text-center">
            <p class="text-colorfull">{{ block.writer }}</p>
        </div>
            {% endif %}
        </div>
    </div>
</div>